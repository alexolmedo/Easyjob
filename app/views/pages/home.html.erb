<div class="home-header text-center container-fluid">
  <h1>Bienvenidos a Easy job</h1>
  <h3>Una comunidad para juntar emprendedores y estudiantes de la Escuela Politécnica Nacional.</h3>
</div>

<!-- Sign up buttons -->
<div class="container home-sign-up">
  <div class="row">
    
    <% if user_signed_in? %>
    
      <!-- User Profile Options -->
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Tu Perfil</h2>
          
          <!-- If the user has created a profile -->
          <% if current_user.profile %>
            <%= link_to "Editar tu perfil", edit_user_profile_path(user_id: current_user.id), class: 'btn btn-info btn-lg btn-block' %>
            <%= link_to "Ver tu perfil", user_path(id: current_user.id), class: 'btn btn-success btn-lg btn-block' %>
          <% else %>
            <p class="text-center">Crea tu perfil para que puedas compartir tu información con la comunidad</p>
            <%= link_to "Crear tu perfil!", new_user_profile_path(user_id: current_user.id), class: 'btn btn-success btn-lg btn-block' %>
          <% end %>
          
        </div>
      </div>
      
      <!-- View the Community -->
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">La Comunidad</h2>
          <p class="text-center">Visita la comunidad para conocer otros estudiantes de la EPN y otros empresarios!</p>
          <%= link_to "Visitar la comunidad", users_path, class: 'btn btn-primary btn-lg btn-block' %>
        </div>
      </div>
    
    <% else %>
      <!-- Basic Membership -->
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Membresía para Estudiantes</h2>
          <h4 class="text-center">Regístrate gratis y consigue un acceso a una variedad de oportunidades de trabajo y pasantías</h4>
          <br>
          
          <!-- Using Query string parameters, we can pass info via URL -->
          <%= link_to "Registro Estudiantes", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-primary btn-lg btn-block' %>
        </div>
      </div>
      
      <!-- Pro Membership -->
      <div class="col-md-6">
        <div class="well">
          <h2 class="text-center">Membresía para Emprendedores</h2>
          <h4 class="text-center">Consigue acceso total a la comunidad de estudiantes calificados para resolver las necesidades de tu empresa</h4>
          <br>
          <%= link_to "Registro Emprendedores", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg btn-block' %>
        </div>
      </div>
    
    <% end %>
    
    
  </div>
</div>
